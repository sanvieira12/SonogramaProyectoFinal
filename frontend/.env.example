# URL base del backend API
# En desarrollo con Vite proxy, dejar vacío (usa /api)
# En producción (Vercel), setear la URL completa del backend en Railway
VITE_API_URL=https://sonograma-backend-production.up.railway.app/api
